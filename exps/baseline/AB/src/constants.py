"""Default constants for AB baseline experiment."""

# Default validation split ratio (for early stopping)
DEFAULT_VAL_SIZE = 0.15

# Default variance threshold for feature selection
DEFAULT_VARIANCE_THRESHOLD = 1e-8

